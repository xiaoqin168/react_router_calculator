module.exports = {
  extends: [
    '../rules/base/best-practices',
    '../rules/base/possible-errors',
    '../rules/base/node',
    '../rules/base/variables',
    '../rules/es5',
    './rules/set-style-to-warn',
    './rules/blacklist',
  ].map(require.resolve),
  rules: {
    // 逗号风格
    // ES5 中不加最后一个逗号
    // @unessential
    'comma-dangle': ['warn', 'never'],

    // 把 var 语句看作是在块级作用域范围之内，不能在块外使用
    // @unessential 这条是 es5 规则，不做强制卡点
    'block-scoped-var': 'warn',
  },
};
